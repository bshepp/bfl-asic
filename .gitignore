__pycache__/
*.pyc
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.claude/

# Default CLI output root (configurable via $BFL_ASIC_OUTPUT_DIR)
runs/

# HF Jobs local staging and results (never commit these)
hf_payload/
hf_results/

# Ad-hoc local artifacts dropped at repo root (use runs/ or -o instead)
/convergence.gif
/dyn.json
/dyn_convergence.png
/dyn_tail_cycle.png
/dyn_orbit_*.png
/stats.json
/stats.png

# Pasted external review notes — not for publication
/docs/tier_b_concerns_and_tier_c_proposal.md

# Raw phone photo originals — only the web-optimized copies in
# docs/images/ are committed (see docs/images/jalapeno-rig.jpg)
/docs/PXL_*.jpg

# Third-party GPLv3 reference (cgminer BFL driver, driver-bflsc.c/.h) — facts-only reference, never committed
/cgminer-ref/
docs/characterization/*.log
